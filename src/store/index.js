import { createStore } from "vuex";

export default createStore({
    modules: {},
    state: () => ({
        title: 'Hello from Store'
    }),
    mutations: {},
    actions: {},
    getters: {},
    // namespaced: true
})

